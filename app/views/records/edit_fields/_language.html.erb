<%#
  # OVERRIDE file from hyrax v2.9.6
  #
%>
<% language_options = LanguagesService.new(controller).select_all_options %>
<%= f.input :language, as: :multi_value_select,
    collection: language_options,
    include_blank: true,
    input_html: { class: 'form-control' } %>
