<% options = DataTypeService.new(controller).select_all_options %>
<%= f.input :data_type, as: :select,
  collection: options,
  input_html: { class: 'form-control', size: options.count, multiple: true }
%>
