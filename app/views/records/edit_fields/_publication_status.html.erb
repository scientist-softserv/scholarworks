<%= f.input :publication_status, as: :select,
  collection: PublicationStatusService.new(controller).select_all_options,
  include_blank: true,
  #item_helper: method(:include_current_value),
  input_html: { class: 'form-control', multiple: false }
%>
