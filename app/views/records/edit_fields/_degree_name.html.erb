<% degree_name_options = DegreeNamesService.new(controller).select_all_options %>
<%= f.input :degree_name, as: :multi_value_select,
    collection: degree_name_options,
    include_blank: true,
    input_html: { class: 'form-control' } %>
