# frozen_string_literal: true

# Usage
# bundle exec rake calstate:metadata_fix

require 'calstate/metadata'

namespace :calstate do
  desc 'Metadata fixer'
  task metadata_fix: :environment do

  end
end
